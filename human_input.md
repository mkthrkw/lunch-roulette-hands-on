# ランチルーレット作りたい

会社とかで昼飯を決めるときに迷うので、候補からランダムで1つ決めてくれるWebアプリが欲しい。

GitHubからzipでダウンロードして使うようなイメージ。
ローカルサーバーを立てたり何かコマンドを実行したりせず、`index.html` をダブルクリックするだけで使えるようにしたい。

DB、API、ログインなどはいらない。
画面を閉じた後までデータを保存する必要もない。

## ランチ候補

最初はこんな感じで6個くらい。

- 🍜 ラーメン
- 🍛 カレー
- 🍣 寿司
- 🍝 パスタ
- 🍱 定食
- 🍲 うどん

候補は画面から追加・削除できるようにしたい。
最低2件は残るようにしてほしい。

候補数が変わったらルーレット側も自動的に変わってほしい。
2件だけの場合も含めて、どの候補数でもラベルが重なったり、文字の向きがおかしくなったりせず、それぞれの候補が正しい位置に表示されるようにしたい。

## ルーレット

画面のメインは大きな円形のルーレットにしたい。
候補ごとに均等に分割されて色分けされた、本当にルーレットっぽい見た目。

ルーレットの上あたりに、どこが選ばれたか分かる矢印がある感じ。

真ん中に「SPIN!」みたいなボタンを置いて、
押したらルーレットが勢いよく回って、2〜3秒くらいかけて徐々に止まってほしい。

止まった位置と実際に選ばれたランチは必ず一致するようにしたい。

止まったら、

「🎉 今日のランチは
🍣 寿司！」

みたいに選ばれたものを大きく表示したい。

## 見た目

勉強会で見せるので見た目は結構大事。

いかにもサンプルHTMLみたいな感じではなく、
ちょっとしたWebサービスっぽくしたい。

イメージとしては、

- 明るくて楽しい感じ
- 背景は薄いオレンジ〜ピンク系のグラデーション
- コンテンツは白いカード
- 角丸
- 適度な影
- ルーレットはパステル系の複数色
- SPINボタンは目立つ色
- 文字は読みやすく
- 絵文字も使いたい

PCでは左側にランチ候補、右側に大きなルーレットを置く感じがいい。

スマホでも見られるようにして、
狭い画面では縦並びになればOK。

回転するときはちゃんとアニメーションしてほしい。
止まったときの結果も、少し拡大しながら表示されるとか、ちょっと演出が欲しい。

ボタンや候補の削除とかも、マウスを乗せたときに少し反応があると嬉しい。

ただし、見た目のために必要以上に複雑にはしたくない。

## 技術について

できるだけシンプルにしたい。

- HTML
- CSS
- JavaScript
- ReactやVueなどのフレームワークは使わない
- Canvasは使わない
- 外部API、DB、外部のUIライブラリは使わない

アプリ本体はローカルサーバーなしで動かしたいので、`index.html` を `file://` で直接開いてもすべての機能が動作する構成にする。

テストにはVitestを使いたい。
テスト実行時にNode.jsやnpmを使うのは問題ない。

## テスト・動作確認

ロジック部分は自動テストしたい。

特に確認したいのは、

- 候補からランダムで1件選べること
- 候補追加ができること
- 候補削除ができること
- 2件未満には削除できないこと
- 選ばれた候補とルーレットの停止位置が矛盾しないこと

自動テストだけではなく、実際のブラウザでも動作確認してほしい。

特に、

- 初期状態の6件
- 候補を追加した状態
- 最低数の2件まで削除した状態
- PC相当の画面幅
- スマホ相当の画面幅
- `index.html` をローカルサーバーなしで直接開いた状態

は確認してほしい。

見た目についても、ラベルの位置や向き、重なり、レイアウト崩れなどがないことを実際の画面で確認してほしい。

今回は小さいアプリなので、必要以上に複雑な構成にはしたくない。
ただ、要件、設計、実装、テストまで一通りちゃんと残したい。
