# Development Rules

## Principles

- `human_input.md` を人間からの要求の一次情報として扱い、変更しない
- 要求 → 設計 → 実装 → テストの順に進め、成果物間の整合性を保つ
- シンプルな設計・実装を優先し、不要な機能・依存関係・抽象化を追加しない
- 重要な不明点は推測で確定せず、既存情報から判断できる範囲で進める
- 仕様変更時は影響範囲を確認し、関連する要件・設計・実装・テストを更新する

## Documentation

- 要件は `docs/requirements.md` に構造化し、要求との整合性を維持する
- 設計は `docs/design.md` に記録する
- ドキュメントは簡潔にし、実装と矛盾させない

## Quality

- 主要なロジックにはテストを作成する
- 変更後はテストを実行し、失敗を放置しない
- テストを通すためだけに要求や仕様を変更しない

## Git

- 作業開始時に `git status` を確認し、既存の変更を破壊しない
- 要件、設計、主要機能、UI改善、テストなどの論理的な作業単位ごとに自動コミットする
- 1回の指示に複数の論理的な作業単位が含まれる場合も、作業単位ごとに自動コミットする
- コミット前に変更内容を確認し、実装変更ではテストを実行する
- コミットメッセージは Conventional Commits に従う
- `push` は明示的に指示されない限り行わない

## Restrictions

- `human_input.md` を変更しない
- 要求されていない大規模なリファクタリングを行わない
- エラーやテスト失敗を無視して完了扱いにしない
- 秘密情報や認証情報をコード・ログ・コミットに含めない
- `HANDS_ON.md` は進行資料であり、参照・変更しない
